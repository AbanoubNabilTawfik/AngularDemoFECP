import { EgyptionPoundPipe } from './egyption-pound.pipe';

describe('EgyptionPoundPipe', () => {
  it('create an instance', () => {
    const pipe = new EgyptionPoundPipe();
    expect(pipe).toBeTruthy();
  });
});
