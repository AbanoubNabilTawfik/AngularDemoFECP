import { CielnumberPipe } from './cielnumber.pipe';

describe('CielnumberPipe', () => {
  it('create an instance', () => {
    const pipe = new CielnumberPipe();
    expect(pipe).toBeTruthy();
  });
});
